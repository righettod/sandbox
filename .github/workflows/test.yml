name: TEST
on:
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: righettod/checkout@v1.0.0
      - name: Test code
        run: |   
          echo "TEST"         
